export function registerSchedulers() {}
// Monthly earn reset is intentionally disabled in wallet-only mode.
// Kept as a no-op export for backward compatibility.
export function registerSchedulers() {
  return;
}
