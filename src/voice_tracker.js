export function registerVoiceTracker() {}
// Engagement rewards are intentionally disabled in wallet-only mode.
// Kept as a no-op export for backward compatibility.
export function registerVoiceTracker() {
  return;
}
